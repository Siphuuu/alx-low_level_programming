#include "main.h"

/**
 * add - add two integer
 * Return: the result
 * @i: the first number
 * @j: the second number
 */
int add(int i, int j)
{
	int i;
	int j;
	int add;

	add = i + j;
	return (add);
}
