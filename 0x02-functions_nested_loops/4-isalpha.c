#include "main.h"

/**
 * _isalpha - checks for alphabetic character
 * @c: the character
 * Return: 1 if its alphabet, 0 if else
 */

int _isalpha(int c)
{
	return ((c >= 'a' && c <= 'z') || (c >= 'A' && C <= 'Z'));
}
